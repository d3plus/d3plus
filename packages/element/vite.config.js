import {defineConfig} from "vite";

// Web components are framework-agnostic, so no framework plugin is needed —
// Vite transpiles the TypeScript entry and resolves the workspace deps.
export default defineConfig({});
